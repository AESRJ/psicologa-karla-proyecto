name: karla_psicologa_admin
description: App de administración — Karla Zermeño Psicóloga

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  table_calendar: ^3.1.2
  intl: ^0.19.0
  shared_preferences: ^2.3.2
  flutter_slidable: ^3.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
